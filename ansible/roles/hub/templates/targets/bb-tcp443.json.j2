{% import "shared/_vff_macros.j2" as vff with context -%}
[
{% for h in groups['vpn'] | default([]) -%}
  { "targets": ["{{ vff.wg_ip(h) }}:443"],
    "labels": { "name": "{{ vff.node_name(h) }}" } }{% if not loop.last %},{% endif %}
{%- endfor %}
]
