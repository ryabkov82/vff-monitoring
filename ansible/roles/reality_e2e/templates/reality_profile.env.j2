# Per-node overrides for reality-e2e@{{ eff_name }}.service
NAME="{{ eff_name }}"
SOCKS_PORT="{{ eff_socks_port }}"

# Optional:
# DEBUG=1
# SLEEP_START=2
# TEST_URL={{ eff_http_url }}
# TIMEOUT={{ eff_http_timeout }}
